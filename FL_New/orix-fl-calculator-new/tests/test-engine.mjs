import assert from'node:assert/strict';import{calculateLease,solveFlatRate}from'../js/engine/finance-engine.js';
const state={document:{},asset:{},pricing:{grossPriceIncVat:3590000,discountIncVat:0,optionIncVat:0,vatPct:7},contract:{termMonths:60,paymentTiming:'arrears'},down:{type:'percent',value:20},balloon:{enabled:true,type:'percent',value:20},calculation:{mode:'IRR',flatRatePct:0,irrTargetPct:5.75},commission:{base:'interest',percent:8,extraAmount:5000},conditions:{},ui:{}};state.calculation.flatRatePct=solveFlatRate(state);const r=calculateLease(state);assert.equal(r.netPrice,3590000);assert.equal(r.downPayment,718000);assert.equal(r.balloon,718000);assert.equal(r.finance,2872000);assert.equal(r.principalForInstallment,2154000);assert.ok(Math.abs(r.companyIRR*100-5.75)<1e-5);assert.equal(r.monthlyRoundedIncVat,45847);assert.equal(r.commission,52745);const arrears=r.companyIRR;state.contract.paymentTiming='advance';const advance=calculateLease(state).companyIRR;assert.ok(advance>arrears);console.log(JSON.stringify({flatRatePct:state.calculation.flatRatePct,monthly:r.monthlyRoundedIncVat,commission:r.commission,companyIRRPct:r.companyIRR*100,customerEffectiveRatePct:r.customerEffectiveRate*100,advanceIRRPct:advance*100},null,2));
